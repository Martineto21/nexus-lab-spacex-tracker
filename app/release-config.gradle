# Release Configuration for SpaceX Tracker
# Generated by Nexus Platform - Día 10 del curso

# Signing configuration
android {
    signingConfigs {
        release {
            storeFile file("../keystore/spacex-tracker-key.jks")
            storePassword "nexus_spacex_2026"
            keyAlias "spacex-tracker"
            keyPassword "nexus_spacex_2026"
        }
    }
    
    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
            
            // Optimizaciones específicas
            debuggable false
            zipAlignEnabled true
            crunchPngs true
        }
    }
}

# Bundle configuration for Play Store
bundle {
    language {
        enableSplit = false
    }
    density {
        enableSplit = true
    }
    abi {
        enableSplit = true
    }
}